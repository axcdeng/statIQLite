import 'dart:convert';
import 'package:http/http.dart' as http;

void main() async {
  // Try fetching events from robotevents API
  // Using a public API or the API used by the app?
  // I need to use the token in the app if it requires one.
}
